# Production Environment (Railway)
NODE_ENV=production

# Database Configuration (Railway PostgreSQL - will be set by Railway)
# DATABASE_URL will be automatically set by Railway PostgreSQL service

# Frontend API URL (Railway Backend)
VITE_API_URL=https://web-production-54d13.up.railway.app

# API Keys (set these in Railway dashboard)
# GEMINI_API_KEY=your_actual_api_key

# JWT Secret (set this in Railway dashboard)
# JWT_SECRET=your-production-jwt-secret

# Frontend URL (Netlify)
FRONTEND_URL=https://scrowx.netlify.app
